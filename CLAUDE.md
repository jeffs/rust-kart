# Code Style Guidelines

- Avoid forward references. Define helper functions before the code that uses them.

## Active Technologies
- Rust (stable toolchain, 2021 edition) + `syn` (Rust parser), `clap` (CLI), `cargo_metadata` (workspace detection) (001-dgmod)
- N/A (stateless, file-based input, stdout output) (001-dgmod)

## Recent Changes
- 001-dgmod: Added Rust (stable toolchain, 2021 edition) + `syn` (Rust parser), `clap` (CLI), `cargo_metadata` (workspace detection)
